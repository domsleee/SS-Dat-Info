diff --git a/TAS/TAS_Helper/src/caves/cave2.hpp b/TAS/TAS_Helper/src/caves/cave2.hpp
index 1eeed44..b25bc0d 100644
--- a/TAS/TAS_Helper/src/caves/cave2.hpp
+++ b/TAS/TAS_Helper/src/caves/cave2.hpp
@@ -240,6 +240,14 @@ static void InjectF5(TasSharedState* s, GameAddresses* addr, uint32_t kbobj, boo
 static volatile uint32_t g_cave2_pendingLog = 0;  // 0=none, 1=REC, 2=PLAY, 3=STOP, 4=playback_done
 static volatile uint32_t g_cave2_logParam = 0;
 
+// Splice gate: 1 only between a SUCCESSFUL CMD_ARM_CONTINUE and its splice
+// (or any stop/re-arm). The PLAY handler's splice check requires this flag,
+// so a `continue_from_frame` that appears in shared memory by any other
+// route (UI bug, stray writer, stale value) can NEVER convert a plain
+// replay into REC. Deliberately a cave2-private static, not a shared-state
+// field — external processes must not be able to set it.
+static volatile uint32_t g_cave2_contArmed = 0;
+
 // Handle command transitions
 // WARNING: NO Log/format/float calls — runs inside SafetyHookMid (x87 FPU not saved).
 static void ProcessCommand(TasSharedState* s) {
@@ -267,6 +275,7 @@ static void ProcessCommand(TasSharedState* s) {
             s->segment_boundaries[0].frame = 0;
             s->segment_boundaries[0].input_log_offset = 0;
             s->mode = MODE_REC;
+            g_cave2_contArmed = 0;
             g_cave2_pendingLog = 1;
             break;
 
@@ -288,6 +297,7 @@ static void ProcessCommand(TasSharedState* s) {
             // subsequent plain PLAY silently hijacks itself into a CONT at
             // the stale frame and truncates/overwrites the recording.
             s->continue_from_frame = 0;
+            g_cave2_contArmed = 0;
 
             // No position forcing — F5 matching must happen naturally.
             // Position forcing (even velocity-preserving) creates physics state
@@ -304,6 +314,8 @@ static void ProcessCommand(TasSharedState* s) {
             if (s->continue_from_frame == 0 || s->continue_from_frame > s->recorded_count) {
                 LogRing(s, LOG_ERROR, "ARM_CONTINUE: invalid splice point");
                 s->mode = MODE_OFF;
+                s->continue_from_frame = 0;  // refused — don't leave a stale marker armed
+                g_cave2_contArmed = 0;
                 g_cave2_pendingLog = 3;  // "stopped"
                 break;
             }
@@ -321,6 +333,8 @@ static void ProcessCommand(TasSharedState* s) {
                 LogRing(s, LOG_ERROR,
                     "ARM_CONTINUE: refused — game is REC/PLAY; CONT requires a fresh restart first");
                 s->mode = MODE_OFF;
+                s->continue_from_frame = 0;  // refused — don't leave a stale marker armed
+                g_cave2_contArmed = 0;
                 g_cave2_pendingLog = 3;  // "stopped"
                 break;
             }
@@ -334,12 +348,20 @@ static void ProcessCommand(TasSharedState* s) {
             s->bb3b10_block_count = 0;
             // Segment tracking: keep existing segment_count, we'll add one at splice
             s->mode = MODE_PLAY;  // Start as PLAY, will auto-switch in PLAY handler
+            g_cave2_contArmed = 1;  // the ONLY place the splice gate opens
             g_cave2_pendingLog = 5;
             break;
 
         case CMD_STOP:
             s->mode = MODE_OFF;
             s->cave2_injecting = 0;
+            // Defense-in-depth: a CONT that was stopped before its splice
+            // fired must not leave a live splice marker behind. Every armer
+            // (controller ArmSettle, harness arm_continue) re-writes the
+            // marker immediately before CMD_ARM_CONTINUE, so clearing here
+            // can't break a legitimate cycle.
+            s->continue_from_frame = 0;
+            g_cave2_contArmed = 0;
             g_cave2_pendingLog = 3;
             break;
 
@@ -483,6 +505,7 @@ static void __declspec(noinline) Cave2_Logic() {
 
         if (pos >= s->recorded_count) {
             s->mode = MODE_OFF;
+            g_cave2_contArmed = 0;  // hygiene — an armed CONT always splices before here
             g_cave2_logParam = pos;
             g_cave2_pendingLog = 4;
             return;
@@ -509,7 +532,11 @@ static void __declspec(noinline) Cave2_Logic() {
         // Continue Record: auto-switch to REC AFTER processing the splice frame.
         // This ensures the splice frame gets normal PLAY processing (input injection
         // + coordinate capture), maintaining symmetry with the final PLAY phase.
-        if (s->continue_from_frame > 0 && s->playback_pos >= s->continue_from_frame) {
+        // Gated on g_cave2_contArmed: only a PLAY entered via CMD_ARM_CONTINUE may
+        // splice. A marker that lands in shared memory by any other route (stray
+        // writer mid-replay, stale value) must never hijack a plain replay into REC.
+        if (g_cave2_contArmed && s->continue_from_frame > 0
+                && s->playback_pos >= s->continue_from_frame) {
             uint32_t splice_pos = s->playback_pos;
             s->recorded_count = splice_pos;
 
@@ -525,6 +552,7 @@ static void __declspec(noinline) Cave2_Logic() {
 
             s->mode = MODE_REC;
             s->continue_from_frame = 0;  // Clear splice marker
+            g_cave2_contArmed = 0;       // splice consumed — close the gate
             g_cave2_logParam = splice_pos;
             g_cave2_pendingLog = 6;
         }
diff --git a/TAS/tas_shared/src/lib.rs b/TAS/tas_shared/src/lib.rs
index 0f6264f..4764d4f 100644
--- a/TAS/tas_shared/src/lib.rs
+++ b/TAS/tas_shared/src/lib.rs
@@ -880,12 +880,24 @@ pub mod transport {
         Unjudged,
     }
 
+    /// How long RestartWaitDone may poll for restart_state==2 before the cycle
+    /// aborts. A restart normally completes in a few seconds; without a bound,
+    /// a cycle whose restart stalls becomes a silent zombie that arms
+    /// REC/PLAY/CONT whenever rs==2 finally shows up — minutes later, when the
+    /// user has long stopped expecting it.
+    pub const RESTART_COMPLETE_TIMEOUT: std::time::Duration =
+        std::time::Duration::from_secs(30);
+
     /// Drives one restart→arm(→judge→reroll) cycle to a terminal outcome.
     pub struct TransportController {
         cfg: ArmConfig,
         phase: Phase,
         retries_remaining: u32,
         completed_via: CompletedVia,
+        /// Wall-clock deadline for the current RestartWaitDone poll; armed when
+        /// the Restart command is sent.
+        restart_deadline: Option<std::time::Instant>,
+        restart_timeout: std::time::Duration,
     }
 
     impl TransportController {
@@ -896,9 +908,17 @@ pub mod transport {
                 phase: Phase::Start,
                 retries_remaining,
                 completed_via: CompletedVia::Unjudged,
+                restart_deadline: None,
+                restart_timeout: RESTART_COMPLETE_TIMEOUT,
             }
         }
 
+        /// Override the restart watchdog (tests).
+        pub fn with_restart_timeout(mut self, timeout: std::time::Duration) -> Self {
+            self.restart_timeout = timeout;
+            self
+        }
+
         pub fn is_terminal(&self) -> bool {
             matches!(self.phase, Phase::Done | Phase::Aborted)
         }
@@ -921,7 +941,12 @@ pub mod transport {
             match self.phase {
                 Phase::Start => {
                     port.set_playback_speed(self.cfg.catchup_speed);
-                    port.set_continue_from_frame(self.cfg.continue_from_frame);
+                    // Do NOT write continue_from_frame here: the game may
+                    // still be in PLAY until the Stop below is processed, and
+                    // cave2's per-frame splice check fires on any non-zero
+                    // marker — writing it pre-Stop could hijack a live replay
+                    // into REC. ArmSettle (game OFF, post-restart) is the
+                    // single marker writer.
                     // Always Stop then settle a FIXED delay before Restart (like
                     // the legacy loop), so the Restart fires at a consistent
                     // wall-clock phase → consistent (good) F5 bucket.
@@ -936,10 +961,28 @@ pub mod transport {
                     // has flipped to OFF. Fire the Restart now.
                     port.reset_restart_state();
                     port.send_command(TasCommand::Restart);
+                    self.restart_deadline =
+                        Some(std::time::Instant::now() + self.restart_timeout);
                     self.phase = Phase::RestartWaitDone;
                     StepOutcome::InProgress
                 }
                 Phase::RestartWaitDone => {
+                    // Watchdog: a restart that never reports rs==2 must abort
+                    // LOUDLY instead of leaving a zombie cycle that silently
+                    // arms whenever a much-later restart completes.
+                    if port.restart_state() != 2 {
+                        if let Some(deadline) = self.restart_deadline {
+                            if std::time::Instant::now() >= deadline {
+                                self.phase = Phase::Aborted;
+                                return StepOutcome::Aborted {
+                                    reason: format!(
+                                        "restart did not complete within {:?}",
+                                        self.restart_timeout
+                                    ),
+                                };
+                            }
+                        }
+                    }
                     if port.restart_state() == 2 {
                         port.reset_restart_state();
                         // Don't arm immediately — honour an arm settle so the arm
@@ -956,7 +999,11 @@ pub mod transport {
                     }
                 }
                 Phase::ArmSettle => {
-                    // cave2 reads these at ARM time — re-assert post-restart.
+                    // cave2 reads these at ARM time. This is the ONLY place
+                    // the splice marker is written: the game is provably OFF
+                    // here (post-restart, rs==2 observed), so a non-zero
+                    // marker can never be seen by a live PLAY that wasn't
+                    // armed as CONT.
                     port.set_continue_from_frame(self.cfg.continue_from_frame);
                     port.set_playback_speed(self.cfg.catchup_speed);
                     port.send_command(self.cfg.arm.command());
@@ -1052,7 +1099,10 @@ pub mod transport {
             }
             self.retries_remaining -= 1;
             let attempt = self.cfg.max_retries - self.retries_remaining;
-            port.set_continue_from_frame(self.cfg.continue_from_frame);
+            // No marker write here: the judged replay is still in PLAY until
+            // this Stop is processed, and re-writing the splice marker against
+            // a live PLAY races cave2's splice check. The next cycle's
+            // ArmSettle re-asserts it before ArmContinue.
             port.set_playback_speed(self.cfg.catchup_speed);
             port.send_command(TasCommand::Stop);
             self.phase = Phase::StopSettle;
@@ -1395,9 +1445,9 @@ pub mod transport {
         #[test]
         fn cont_drives_splice_frame_to_the_game() {
             // "Continue must continue on the correct frame": the controller has
-            // to push continue_from_frame (the splice frame) into the game both
-            // before arming AND re-assert it on every reroll, or cave2 splices
-            // at the wrong tick. cfg() sets continue_from_frame = 1000 for CONT.
+            // to push continue_from_frame (the splice frame) into the game
+            // before arming AND re-assert it on every reroll cycle, or cave2
+            // splices at the wrong tick. cfg() sets continue_from_frame = 1000.
             let target = BucketTarget {
                 expected_start_bits: bits(1.0, 2.0, 3.0),
                 expected_first_moving: Some(250),
@@ -1418,7 +1468,9 @@ pub mod transport {
             );
 
             // Force a wrong bucket so the controller rerolls, after corrupting
-            // the frame — the reroll must restore it.
+            // the frame — the next cycle's ArmSettle must restore it. (The
+            // reroll's Stop itself must NOT write the marker: the judged
+            // replay is still live in PLAY at that instant.)
             let mut wrong = vec![[1.0f32, 2.0, 3.0]; 400];
             wrong[248] = [1.0, 2.0, 3.5];
             p.play_coords = wrong;
@@ -1428,9 +1480,79 @@ pub mod transport {
                 StepOutcome::Reroll { .. } => {}
                 other => panic!("expected Reroll, got {:?}", other),
             }
+            assert_eq!(
+                p.continue_from_frame, 0,
+                "marker must not be written against the still-live PLAY at reroll"
+            );
+            // Drive the reroll cycle through restart to the re-arm.
+            p.mode = TasMode::Off as u32; // Stop processed during settle
+            c.step(&mut p); // StopSettle -> Restart
+            p.restart_state = 2;
+            c.step(&mut p); // RestartWaitDone -> ArmSettle (Wait)
+            c.step(&mut p); // ArmSettle -> ArmContinue
+            assert_eq!(p.commands.last(), Some(&TasCommand::ArmContinue));
             assert_eq!(
                 p.continue_from_frame, 1000,
-                "splice frame not re-asserted on reroll"
+                "splice frame not re-asserted on the reroll's re-arm"
+            );
+        }
+
+        #[test]
+        fn splice_marker_not_written_while_game_in_play() {
+            // Regression: pressing CONT while a replay is PLAYing must not
+            // make the splice marker visible to the live PLAY before the
+            // Stop lands — cave2's per-frame splice check would hijack the
+            // replay into REC at the marker (truncating recorded_count).
+            // The marker may only be written in ArmSettle, when the game is
+            // OFF post-restart.
+            let mut p = FakePort {
+                mode: TasMode::Play as u32,
+                playback_pos: 6200, // playhead already past the splice frame
+                ..Default::default()
+            };
+            let mut c = TransportController::new(cfg(Arm::Continue, None, 0));
+
+            // Start: Stop is sent — the marker must still be untouched.
+            c.step(&mut p);
+            assert_eq!(p.commands, vec![TasCommand::Stop]);
+            assert_eq!(
+                p.continue_from_frame, 0,
+                "splice marker written while the game was still in PLAY"
+            );
+
+            // Stop processed → OFF; restart; only then may the marker land.
+            p.mode = TasMode::Off as u32;
+            c.step(&mut p); // StopSettle -> Restart
+            assert_eq!(p.continue_from_frame, 0);
+            p.restart_state = 2;
+            c.step(&mut p); // RestartWaitDone -> ArmSettle (Wait)
+            assert_eq!(p.continue_from_frame, 0);
+            c.step(&mut p); // ArmSettle -> ArmContinue
+            assert_eq!(p.commands.last(), Some(&TasCommand::ArmContinue));
+            assert_eq!(p.continue_from_frame, 1000);
+        }
+
+        #[test]
+        fn play_clears_stale_splice_marker_before_arming() {
+            // A stale marker left in shared (e.g. an aborted CONT) must be
+            // overwritten with 0 by the time ArmPlay is sent, so the plain
+            // replay can't splice into REC at the stale frame.
+            let mut p = FakePort {
+                mode: TasMode::Off as u32,
+                continue_from_frame: 6000, // stale from a dead CONT
+                ..Default::default()
+            };
+            let mut c = TransportController::new(cfg(Arm::Play, None, 0));
+            c.step(&mut p); // Start: Stop
+            p.mode = TasMode::Off as u32;
+            c.step(&mut p); // Restart
+            p.restart_state = 2;
+            c.step(&mut p); // -> ArmSettle (Wait)
+            c.step(&mut p); // ArmSettle -> ArmPlay
+            assert_eq!(p.commands.last(), Some(&TasCommand::ArmPlay));
+            assert_eq!(
+                p.continue_from_frame, 0,
+                "stale splice marker survived into a plain PLAY"
             );
         }
 
diff --git a/TAS/tas_ui/src/main.rs b/TAS/tas_ui/src/main.rs
index 79d502b..1f7f9a9 100644
--- a/TAS/tas_ui/src/main.rs
+++ b/TAS/tas_ui/src/main.rs
@@ -762,11 +762,17 @@ impl TasApp {
                 }
             }
             transport::Action::SetContinueFrame(frame) => {
+                // Stage the splice frame UI-side ONLY. Writing it straight
+                // into shared memory here armed cave2's per-frame splice
+                // check while the game could still be in PLAY (the CONT
+                // button is enabled during PLAY): if the playhead was at or
+                // passed the frame before the controller's Stop landed
+                // (≥1 egui frame later), the replay flipped to REC mid-watch
+                // and truncated recorded_count. The TransportController is
+                // the only writer now — it asserts the marker in ArmSettle,
+                // post-restart, when the game is provably OFF.
                 self.continue_from_frame = frame;
                 self.continue_from_text = frame.to_string();
-                if let Some(shared) = self.shared.as_mut() {
-                    shared.state_mut().continue_from_frame = frame;
-                }
             }
             transport::Action::StepOne => {
                 self.log_lines
@@ -2294,9 +2300,12 @@ impl eframe::App for TasApp {
                 // auto-switches PLAY → REC at that frame. If we synced
                 // every frame, then editing the "From:" textbox or dragging
                 // the timeline continue marker during PLAY would silently
-                // hijack PLAY into a CONT-like splice. Shared is written
-                // only when CONT is actually armed (queue_restart_then,
-                // SetContinueFrame action, retry paths).
+                // hijack PLAY into a CONT-like splice. The ONLY writer is
+                // the TransportController's ArmSettle phase, post-restart,
+                // when the game is OFF. (SetContinueFrame used to write
+                // shared directly on click — pressing CONT during PLAY with
+                // the playhead past the frame hijacked the live replay into
+                // REC before the Stop landed.)
 
             }
         });
